#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
	ll h, w;
	scanf("%lld %lld", &h, &w);
	ll a[h][w], b[h][w];
	for(ll i=0; i<h; i++) for(ll j=0; j<w; j++) scanf("%lld", &a[i][j]);
	for(ll i=0; i<h; i++) for(ll j=0; j<w; j++) scanf("%lld", &b[i][j]);

	ll dp[h][w][520000];
	for(ll i=0; i<h; i++){
		for(ll j=0; j<w; j++){
			
		}
	}
	
	return 0;
}


